
import { Task, calculateProgress, getDependencyTitles } from "./TaskTypes";
import { tasks } from "./data/tasks";

// Re-export the calculate progress function
export { calculateProgress };

// Re-export the getDependencyTitles function
export { getDependencyTitles };

// Re-export the tasks array
export { tasks };
